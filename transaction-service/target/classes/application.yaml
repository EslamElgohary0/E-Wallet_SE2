server:
  port: 8083

spring:
  application:
    name: transaction-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/ewallet_transactions
      database: ewallet_transactions

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

jwt:
  secret: ewallet-super-secret-key-2024-must-be-at-least-256-bits-long!!

management:
  endpoints:
    web:
      exposure:
        include: health,info